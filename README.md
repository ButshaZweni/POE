I added CSS for the global styles (body, headings, text colors).
I added CSS for the navigation bar (background, links, hover effects).
I added CSS for the hero section (full‑screen image, overlay, centered text).
I added CSS for the cards (image containers with shadows and rounded corners).
I added CSS for the grid layouts (responsive design using CSS Grid).
I added CSS for the forms and buttons (inputs styled, green action buttons with hover effects).
I added CSS for the footer (dark background, links, flexbox layout, hover colors).
